#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    vector<int> v(n);
    for (auto i : v)
    {
        cin >> v[i];
    }
    replace(v.begin(), v.end(),);
    for (auto i : v)
    {
        cout << v[i] << " ";
    }

    return 0;
}